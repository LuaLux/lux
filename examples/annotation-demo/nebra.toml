name = "annotation-demo"
version = "0.1.0"
target = "5.4"
source = "src"
output = "out"
entry = "main.neb"
annotations = ["annotations"]
generate_declarations = false

[code]
# Annotation `apply` scripts read IR via Lua's native 1-based indexing
# (`target.namePath[1]`), so keep the project's index base aligned with that.
index_base = 1
